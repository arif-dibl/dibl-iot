# ============================================
# DIBL IoT - Coolify Environment Variables
# ============================================
# এই ফাইলটি একটি টেম্পলেট - Coolify-তে এই ভ্যালুগুলো সেট করতে হবে
#
# Option 1: sslip.io (নিজস্ব ডোমেইন নেই)
# ----------------------------------------
# 1. প্রথমে Coolify-তে প্রজেক্ট তৈরি করুন
# 2. Coolify একটি auto-generated domain দেবে (যেমন: abc123.192.168.1.100.sslip.io)
# 3. সেই domain নিচে OR_HOSTNAME-এ বসান
# 4. Deploy করুন
#
# Option 2: Custom Domain (নিজস্ব ডোমেইন আছে)
# -------------------------------------------
# OR_HOSTNAME=your-domain.com
# DNS-এ A Record সেট করুন

# =========== REQUIRED ===========
# Coolify-এর দেওয়া domain বা নিজস্ব domain
# উদাহরণ: abc123.192.168.1.100.sslip.io
OR_HOSTNAME=

# Admin পাসওয়ার্ড (শক্তিশালী পাসওয়ার্ড দিন)
OR_ADMIN_PASSWORD=

# =========== OPTIONAL ===========
# SSL certificate-এর জন্য ইমেইল
OR_EMAIL_ADMIN=

# SSL Port (-1 for default)
OR_SSL_PORT=-1

# Development mode (production-এ false রাখুন)
OR_DEV_MODE=false

# Metrics enable/disable
OR_METRICS_ENABLED=true

# Docker image versions (latest ব্যবহার করতে পারেন)
PROXY_VERSION=latest
POSTGRESQL_VERSION=latest
KEYCLOAK_VERSION=latest
MANAGER_VERSION=latest
